console.log(Math);  //math object are bundled together with many const and methods.
console.log(Math.PI)

const area=7.2;
const area1=4.2;
const x= 5.6

console.log(Math.round(area)); //round of the number

console.log(Math.floor(area1)); //It remove the decimal number 

console.log(Math.ceil(x));  //It ciel the number to highest end.

const random = Math.random(); //It genrate the random number between 0 to 1

console.log(random);
console.log(Math.round(random*100)); //It will generate random runber between 0 to 100

